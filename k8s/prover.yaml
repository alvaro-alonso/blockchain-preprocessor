apiVersion: v1
kind: Service
metadata:
  namespace: st-alonso
  name: prover-service
  labels:
    app: prover-node
spec:
  type: NodePort
  ports:
  - port: 8000
    protocol: TCP
  selector:
    app: prover-node 
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prover-deployment
  namespace: st-alonso
  labels:
    app: prover-node
spec:
  replicas: 3
  selector:
    matchLabels:
      app: prover-node
  template:
    metadata:
      labels:
        app: prover-node
    spec:
      containers:
      - name: zokrates
        image: alvaround/zokrates-api:latest
        imagePullPolicy: IfNotPresent # use local image if downloaded ex: $ minikube ssh docker pull <IMAGE>
        resources:
          requests:
            memory: "100Mi"
            cpu: "250m"
          limits:
            memory: "500Mi"
            cpu: "500m"
        ports:
        - containerPort: 8000
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
            httpHeaders:
            - name: X-Custom-Header
              value: Awesome
          initialDelaySeconds: 60
          failureThreshold: 3
          periodSeconds: 10